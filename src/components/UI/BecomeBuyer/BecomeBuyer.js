import React from "react";
import cl from "./BecomeBuyer.module.css"


const BecomeBuyer = (visible) => {

    return (
        <section className={cl.root} style={{display: visible}}>
           
        </section>
    );
}

export default BecomeBuyer;